env: "local" # local, dev, prod
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8082"
  timeout: 4s # time for read/write request, not for processing (4s for each)
  idle_timeout: 60s
